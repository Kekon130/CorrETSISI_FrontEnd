import { useCallback, useState } from "react";
import { CorrectExam } from "../Services/Examns";

export function useExams({ form }) {
  const [loading, setLoading] = useState(false)

  const getExamen = useCallback()
}